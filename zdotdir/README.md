### ZDOTDIR

Use this folder for zsh:

```
./zdotdir/link.sh
```

#### Load order

```
# zsh -l
.zshenv → .zprofile → .zshrc → .zlogin → .zlogout

→ ~/.zshenv
→ /etc/zprofile
→ /etc/zshrc
→ /etc/zshrc_Apple_Terminal
→ ~/.zshrc
```
